# Environment Variables for Production
# Copy these to Vercel Dashboard: Settings > Environment Variables

# Database - Use SQLite in memory for initial deploy
DATABASE_URL=sqlite:///:memory:

# JWT Secret - Generate a new one for production later
JWT_SECRET=temporary_jwt_secret_key_change_in_production_32chars

# Encryption Key - Must be exactly 32 characters
ENCRYPTION_KEY=temp_encryption_key_32_chars!!

# App URL - Update after deployment
NEXT_PUBLIC_APP_URL=https://your-project.vercel.app

# Optional - Only if using integrations
# SLACK_CLIENT_ID=
# SLACK_CLIENT_SECRET=
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
