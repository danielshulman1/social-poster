{
  "name": "business-app-monorepo",
  "version": "1.0.0",
  "packageManager": "pnpm@10.25.0",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r run build",
    "dev": "pnpm -r run dev",
    "test": "pnpm -r run test"
  },
  "devDependencies": {
    "@nestjs/config": "^3.3.0",
    "@nestjs/throttler": "^4.2.1",
    "@types/bcrypt": "^5.0.2",
    "@types/cors": "^2.8.19",
    "bcrypt": "^5.1.1",
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.4",
    "cors": "^2.8.5",
    "helmet": "^7.2.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "uuid": "^13.0.0"
  }
}